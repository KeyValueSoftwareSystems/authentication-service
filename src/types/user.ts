export interface User {
  id: string;
  email: string;
  phone: string;
  firstName: string;
  middleName: string;
  lastName: string;
}

export interface Group {
  id: string;
  name: string;
}
export interface Permission {
  id: string;
  name: string;
}
